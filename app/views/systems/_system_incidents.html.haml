%button.btn.btn-danger.modal-X-btn
  X
%br
%h1 
  Status of 
  =@system_decorator.name 
=render('incidents/incident')  
=render('incidents/incident_history')
%button.btn.btn-primary.modal-close-btn
  Close Window

:javascript
  var DM={
    setup: function() {
      $('.modal-close-btn, .modal-X-btn').on('click', function() {
        $('#details-modal').html('');
        $('#details-modal').hide();
      });
    }
  }

  $(DM.setup);