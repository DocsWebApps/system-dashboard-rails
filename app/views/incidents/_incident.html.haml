=image_tag "#{@system_decorator.status}.png"
%hr
%h2
  Incidents Occuring in the last 24 hours
- if @system_decorator.incidents.empty?
  %h3
    No P1 or P2 incidents during the last 24 hours
- else  
  .my-table
    %table.table.table-condensed
      %tr
        %th Fault Reference
        %th Description
        %th Start Date
        %th Start Time
        %th Severity
        %th Status
      - @system_decorator.incidents.order(date: :desc).each do |incident|
        %tr
          %td
            =incident.hp_ref  
          %td
            =incident.description
          %td
            =incident.date.to_formatted_s(:rfc822)
          %td
            =incident.time.to_formatted_s(:time)              
          %td
            =incident.severity       
          %td
            =incident.status 